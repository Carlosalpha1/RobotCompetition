cmake_minimum_required(VERSION 3.0.2)
project(robot_competition_scene)
find_package(catkin REQUIRED COMPONENTS urdf xacro)
catkin_package(CATKIN_DEPENDS gazebo_ros)
include_directories(${catkin_INCLUDE_DIRS})
